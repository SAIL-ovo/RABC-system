spring:
  datasource:
    url: jdbc:mysql://localhost:3306/permission_management?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: thg20030627
    driver-class-name: com.mysql.cj.jdbc.Driver
  redis:
    host: localhost
    port: 6379
    password:
    database: 0
    timeout: 5000ms
    lettuce:
      pool:
        # 连接池配置先保持保守值，足够当前权限系统使用，后续可再按并发量调整。
        max-active: 8
        max-idle: 8
        min-idle: 0
        max-wait: 3000ms

mybatis-plus:
  type-aliases-package: com.example.entity
  global-config:
    db-config:
      id-type: auto
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

server:
  port: 8080
